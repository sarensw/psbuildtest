name: Build/release

on: push

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest]

    steps:
      - name: Display the path
        run: echo ${env:PATH}
        shell: pwsh
      - name: Build Win
        shell: pwsh
        if: startsWith(matrix.os, 'windows')
        env:
          NODE_ENV: production
        run: |
          .\build_win.ps1
